<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.persongaldiary.mapper.IUser">
    <insert id="userRegister">
        insert into `user`(username,password,address,telephone,email) values (#{username},#{password},#{address},#{telephone},#{email})
    </insert>
</mapper>
